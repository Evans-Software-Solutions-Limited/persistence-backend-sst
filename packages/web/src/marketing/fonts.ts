// Self-hosted marketing fonts (fontsource) — matches the repo's font strategy
// (see @fontsource-variable/figtree in index.css) rather than an external
// Google Fonts call. Imported once by MarketingLayout.
import "@fontsource-variable/geist";
import "@fontsource-variable/geist-mono";
import "@fontsource/instrument-serif/400.css";
import "@fontsource/instrument-serif/400-italic.css";
