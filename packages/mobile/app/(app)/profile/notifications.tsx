import { NotificationPreferencesContainer } from "@/ui/containers/NotificationPreferencesContainer";

/**
 * Notification preferences route — linked from the ProfileDrawer
 * "Notifications" row (08-profile-settings STORY-006).
 *
 * Spec: specs/09-notifications-social/requirements.md STORY-003 AC 3.1
 */
export default function NotificationPreferencesScreen() {
  return <NotificationPreferencesContainer />;
}
