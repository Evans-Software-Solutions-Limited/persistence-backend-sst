import { HealthSettingsContainer } from "@/ui/containers/HealthSettingsContainer";

/**
 * Health & integrations route — linked from the ProfileDrawer
 * "Health & integrations" row (08-profile-settings) and serving the
 * 07-health-integration connect flow.
 */
export default function HealthSettingsScreen() {
  return <HealthSettingsContainer />;
}
